/* import { LanguageService, LoadingAnimationPopupOptions, Popup, Policy, PolicyShort } from '.';
import { TermsClient } from './services/terms/terms.service';


export interface MT {
    components: {
        popup: Popup;
    };
    services: {
        languageService: LanguageService;
        loadingAnimationPopupOptions: LoadingAnimationPopupOptions;
        termsClient: TermsClient;
        policy: Policy;
        policyShort: PolicyShort;
    };
}


declare global {
    interface Window {
        mt: MT;
    }
}
 */
